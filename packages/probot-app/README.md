# Probot App

Placeholder adapter for a future GitHub App.

The Probot adapter should reuse `packages/core` instead of reimplementing automation behavior. It is intentionally deferred until the JavaScript action interface is proven by one or more SDK pilots.
