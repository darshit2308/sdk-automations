{
  "name": "sdk-automations",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Hiero SDK GitHub automation core and adapters",
  "workspaces": [
    "packages/core",
    "packages/github-action-adapter",
    "packages/probot-app"
  ],
  "scripts": {
    "build": "npm run build --workspace @hiero-sdk-automations/github-action-adapter",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
